#include "server.h"

typedef struct header header;

typedef struct {
	void* item;
	node* next;
} node;

typedef struct {

	node* tail;
	node* head;
} header;


void push(void* item);
void* pop();
int isEmpty();
void* search(int fileId);

